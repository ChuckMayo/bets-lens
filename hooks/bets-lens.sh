#!/bin/bash
# Bets-lens reminder — injected on every prompt (UserPromptSubmit).
# Standing rule: don't enforce the bets framework, surface it at decision
# time. Keep the line SHORT — it rides every prompt.
echo 'Bets lens (standing rule): if this prompt kicks off new substantive work, state the implied bet ("we believe X will cause Y") and its level of knowing (1-5, 5=measured) before building, and challenge level 1-2 bets. Discuss through this framing; never block shipping when the user says go.'
