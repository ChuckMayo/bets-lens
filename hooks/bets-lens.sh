#!/bin/bash
# Bets-lens reminder — injected on every prompt (UserPromptSubmit).
# Standing rule: don't enforce the bets framework, surface it at decision
# time. Keep the line SHORT — it rides every prompt.
echo 'Bets lens (standing rule): if this prompt kicks off new substantive work, state the implied bet ("we believe X will cause Y") and how much we actually know — Opinion, Signal, Evidence, Validated, or Measured — before building, and challenge bets resting on Opinion or Signal. Discuss through this framing; never block shipping when the user says go.'
