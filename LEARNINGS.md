# bets-lens — Learnings

Provisional notes captured by the **Self-Learning** loop defined in `SKILL.md` (same directory).
Curate, don't accumulate: replace stale or contradicted entries, dedupe, and delete what no
longer holds. Durable notes get promoted into `SKILL.md` and removed from here — run `/skill-audit`
to sweep.

Format (newest first):

```
- YYYY-MM-DD — <trigger/context>: <what failed> → <what works now> (why)
```

---

_(no learnings recorded yet)_
